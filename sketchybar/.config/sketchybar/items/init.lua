-- ========== 加载所有状态栏条目（从左到右排列） ==========
require("helpers.borders") -- 中央调色器（动态分配边框颜色）
require("items.apple") --  Apple logo（最左）
require("items.spaces") -- aerospace 工作区（内含异步加载 front_app）
require("items.calendar") -- 日期时间
require("items.widgets") -- 右侧小部件组
