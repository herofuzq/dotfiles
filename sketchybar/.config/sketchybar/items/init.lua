-- ========== 加载所有状态栏条目（从左到右排列） ==========
require("items.apple") --  Apple logo（最左）
require("items.spaces") -- aerospace 工作区
require("items.calendar") -- 日期时间
require("items.widgets") -- 右侧小部件组
require("items.front_app") -- 当前前台应用名
require("items.media") -- 当前播放媒体
